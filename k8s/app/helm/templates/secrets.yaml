apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm.fullname" . }}-secrets
  labels:
    {{- include "helm.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
