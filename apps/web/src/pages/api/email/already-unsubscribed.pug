html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Unsubscribed
body(style="font-family: sans-serif; text-align: center; margin-top: 15vh;")
  h2 #{title}
  h3 #{email}
  p It looks like you're already unsubscribed.
  p Want to start receiving these emails again?
  form(method="POST")
    button(type="submit" name="resubscribe" value="true") Resubscribe
