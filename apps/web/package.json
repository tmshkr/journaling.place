{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "next build && ./postbuild.sh",
    "dev": "next dev",
    "start": "node .next/standalone/apps/web/server.js",
    "lint": "next lint",
    "jest": "jest",
    "jest:watch": "jest --watch"
  },
  "dependencies": {
    "ui": "*"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "tsconfig": "*"
  }
}
