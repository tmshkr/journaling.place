{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "next build && sh postbuild.sh",
    "dev": "next dev",
    "start": "node .next/standalone/apps/web/server.js",
    "lint": "next lint",
    "jest": "jest",
    "jest:watch": "jest --watch"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "tsconfig": "*"
  }
}
