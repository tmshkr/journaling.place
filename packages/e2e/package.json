{
  "name": "e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "install:ci": "npm run remove-generator && npm install && npm run install:playwright && prisma generate",
    "install:playwright": "playwright install --with-deps chromium",
    "remove-generator": "sed -i '/generator erd/,/}/d' ../../prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.5.2",
    "mongodb": "^6.2.0",
    "next-auth": "^4.24.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0",
    "@types/node": "^20.8.10",
    "dotenv": "^16.3.1",
    "prisma": "^5.5.2"
  },
  "prisma": {
    "schema": "../../prisma/schema.prisma"
  }
}
